# 환경변수 / 시크릿
.env
.env.compose

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/

# Vector store (로컬 인덱싱 결과)
.evidence_store/

# uv
# uv.lock 은 커밋합니다 (환경 재현). 아래만 무시.
.uv/

# LangGraph Studio 로컬 실행 데이터
.langgraph_api/

# IDE / OS
.idea/
.vscode/
.DS_Store

# Claude Code 로컬 설정
.claude/
CLAUDE.md

# push 전 actions 오류 로컬 체크 용
check.bat

# Certbot이 생성하는 SSL 인증서/설정
certbot/

# 로컬 Chroma 벡터 DB (더 이상 사용 안 함, pgvector로 전환됨)
.chroma/